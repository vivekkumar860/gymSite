export type AdminExerciseFilterState = {
  search: string;
  primaryMuscle: string;
  equipment: string;
  difficulty: string;
  isActive: string;
};

export type AdminUserFilterState = {
  search: string;
  role: string;
  accountStatus: string;
};
